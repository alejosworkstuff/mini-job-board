{
  "name": "mini-job-board",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend job board with filtering, state handling, and persistent UI preferences.",
  "homepage": "https://alejosworkstuff.github.io/mini-job-board/",
  "keywords": [
    "portfolio",
    "javascript",
    "typescript",
    "vanilla-js",
    "playwright"
  ],
  "scripts": {
    "check:syntax": "node --check app.js && node --check job-details.js && node --check saved-jobs.js && node --check site-header.js && node --check jobs-fetch-error.js",
    "check:data": "node scripts/validate-jobs.mjs",
    "test": "node --test tests/*.mjs",
    "test:e2e": "playwright test",
    "ci": "npm run check:syntax && npm run check:data && npm run test",
    "ci:full": "npm run ci && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "serve": "^14.2.4",
    "sharp": "^0.35.1"
  }
}
